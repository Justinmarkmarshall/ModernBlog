apiVersion: v1
kind: Service
metadata:
  name: {{ include "modernblog.fullname" . }}
  labels:
    {{- include "modernblog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "modernblog.selectorLabels" . | nindent 4 }}
